// Re-exported from the shared hooks layer so base-level components can consume it
// without an upward base -> crud dependency. The implementation lives in src/hooks.
export { useMediaQuery } from "../../hooks/use-media-query";
