import { {{EntityPascal}}FormScreen } from "{{modulePkgName}}";
import { useTranslation } from "@simplix-react/i18n/react";
import { Stack, useLocalSearchParams, useRouter } from "expo-router";

export default function {{EntityPascal}}EditRoute() {
  const { t } = useTranslation("{{moduleNamespace}}/widgets");
  const router = useRouter();
  const { id } = useLocalSearchParams<{ id: string }>();

  return (
    <>
      <Stack.Screen options={{ldb}} title: t("{{entity}}.actions.edit") {{rdb}} />
      <{{EntityPascal}}FormScreen {{entity}}Id={id} onDone={() => router.back()} />
    </>
  );
}
