import type { MockDomainConfig, MockHandlerOptions } from "@simplix-react/mock";
import { createHandlers } from "./handlers";
import { seed } from "./seed";

export function create{{pascalCase domainName}}Mock(options?: MockHandlerOptions): MockDomainConfig {
  return {
    name: "{{domainName}}",
    handlers: createHandlers(options),
    seed,
  };
}
