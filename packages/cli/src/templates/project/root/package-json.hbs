{
  "name": "{{scope}}/{{projectName}}-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run clean && turbo run build",
    "dev": "turbo run clean && turbo run build && turbo watch dev:app",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "oxlint": "{{deps.oxlint}}",
    "simplix-react": "catalog:",
    "turbo": "{{deps.turbo}}",
    "typescript": "{{deps.typescript}}"
  }
}
