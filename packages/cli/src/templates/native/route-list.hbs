import { {{EntityPascal}}ListScreen } from "{{modulePkgName}}";
import { useTranslation } from "@simplix-react/i18n/react";
import { Stack, useRouter } from "expo-router";

export default function {{EntityPascal}}ListRoute() {
  const { t } = useTranslation("{{moduleNamespace}}/widgets");
  const router = useRouter();

  return (
    <>
      <Stack.Screen options={{ldb}} title: t("{{entity}}.title") {{rdb}} />
      <{{EntityPascal}}ListScreen
{{#if hookGet}}
        onOpenDetail={(id) => router.push(`/{{entityKebab}}/${id}`)}
{{/if}}
{{#if hookCreate}}
        onCreate={() => router.push("/{{entityKebab}}/new")}
{{/if}}
      />
    </>
  );
}
