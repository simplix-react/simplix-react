import { {{EntityPascal}}FormScreen } from "{{modulePkgName}}";
import { useTranslation } from "@simplix-react/i18n/react";
import { Stack, useRouter } from "expo-router";

export default function {{EntityPascal}}CreateRoute() {
  const { t } = useTranslation("{{moduleNamespace}}/widgets");
  const router = useRouter();

  return (
    <>
      <Stack.Screen options={{ldb}} title: t("{{entity}}.actions.create") {{rdb}} />
      <{{EntityPascal}}FormScreen onDone={() => router.back()} />
    </>
  );
}
